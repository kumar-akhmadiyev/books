<%= form_for(@quotation) do |f| %>
  <% if @quotation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quotation.errors.count, "error") %> prohibited this quotation from being saved:</h2>

      <ul>
      <% @quotation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
      <input type="text" name="quotation[body]" class="contacttextform" placeholder="Текст цитаты" value="<%= @quotation.body %>"/>
  </fieldset>
  <fieldset>
      <input type="text" name="quotation[author]" class="contacttextform" placeholder="Автор цитаты" value="<%= @quotation.author %>"/>
  </fieldset>
  <fieldset>
    <input name="commit" class="largesmoothrectange orangebutton" type="submit" value="Готово"/>
  </fieldset>
<% end %>
